apiVersion: v1
kind: ConfigMap
metadata:
  name: netops-lab-config
  namespace: netops-lab
data:
  HTTP_ADDR: ":8080"
  TCP_ADDR: ":9090"
  UDP_ADDR: ":9091"
  LOG_LEVEL: "info"
  QUEUE_CAPACITY: "4096"
  DLQ_CAPACITY: "512"
  WORKERS: "8"

